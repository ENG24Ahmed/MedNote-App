<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- أذونات عامة للإشعارات والمنبّهات الدقيقة وإعادة الجدولة بعد إعادة التشغيل -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- (اختياري) للسماح بتجاهل تحسينات البطارية من الإعدادات -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name="${applicationName}"
        android:label="mednote"
        android:icon="@mipmap/ic_launcher">

        <!-- الشاشة الرئيسية للتطبيق -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- يستقبل رنين المنبّه ويعرض إشعار الجرعة (دواء) -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false" />

        <!-- أزرار الإشعار (أخذت/سنوك) للدواء -->
        <receiver
            android:name=".ActionReceiver"
            android:exported="false" />

        <!-- إعادة جدولة/تهيئة بعد إعادة تشغيل الجهاز (اختياري) -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- ريسيفر إشعارات التذكيرات/المواعيد (جديد) -->
        <receiver
            android:name=".ReminderReceiver"
            android:exported="false" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- السماح باستدعاء نوايا مشاركة/معالجة نص إن لزم -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
